export class User {
    constructor (
        fullName,
        avatar,
        email,
        password,
        userId,
        creationDate
    ) {
        this.fullName = fullName
        this.avatar = avatar
        this.email = email
        this.password = password
        this.userId = userId
        this.creationDate = creationDate
    }
}
