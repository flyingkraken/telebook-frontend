export class UserTie {
  constructor (
    userId,
    creationDate,
    fullName,
    avatar,
    approved,
    circleIdList
  ) {
    this.userId = userId
    this.creationDate = creationDate
    this.fullName = fullName
    this.avatar = avatar
    this.approved = approved
    this.circleIdList = circleIdList
  }

}
