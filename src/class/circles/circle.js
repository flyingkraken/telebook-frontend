export class Circle  {
  constructor (
    id,
    creationDate,
    ownerId,
    name,
    isSystem
  ) {
    this.id = id
    this.creationDate = creationDate
    this.ownerId = ownerId
    this.name = name
    this.isSystem = isSystem
  }

}
